package main

import "testing"

func TestDaemonPackageBuilds(t *testing.T) {
	// The daemon is intentionally exercised through package compilation here.
	// Provider integration tests remain mock-only until adapter contracts are reviewed.
}
